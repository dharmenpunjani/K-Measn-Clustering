This is the K-Means Clustering Project.
You can run the program as follows:
Usage:java Kmeans inputfile(fullpath) outputfile(fullpath) Seed(Base for random number (numeric value))
The input file must be in CSV( Comma saperated Value) in which the last attribute is to be considered as a class/cluster lable

The sample input files you can find wine.txt and iris.txt
